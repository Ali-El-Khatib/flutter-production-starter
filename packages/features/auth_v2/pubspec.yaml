name: feature_auth_v2
description: Alternative authentication feature demonstration brick (Zero-blast-radius LEGO replaceability).
version: 0.0.1
publish_to: 'none'

environment:
  sdk: '>=3.6.0 <4.0.0'
  flutter: '>=3.0.0'

resolution: workspace

dependencies:
  flutter:
    sdk: flutter

  # Shared Contracts & Storage
  auth_contract: ^0.0.1
  app_core: ^0.0.1
  app_storage: ^0.0.1
  get_it: ^9.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  app_lints: ^0.0.1
