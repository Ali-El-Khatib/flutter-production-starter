name: auth_contract
description: Stable authentication contracts, entities, and repository interfaces shared by interchangeable auth implementations.
version: 0.0.1
publish_to: 'none'

environment:
  sdk: '>=3.6.0 <4.0.0'

resolution: workspace

dependencies:
  app_core: ^0.0.1
  meta: ^1.15.0

dev_dependencies:
  app_lints: ^0.0.1
  test: ^1.25.8
