name: app_core
description: "Core abstractions, results, failures, exceptions, and logging."
version: 0.0.1
publish_to: 'none'

environment:
  sdk: '>=3.6.0 <4.0.0'

resolution: workspace

dependencies:
  logger: ^2.7.0
  meta: ^1.15.0

dev_dependencies:
  app_lints: ^0.0.1
  test: ^1.25.8
