name: app_storage
description: "Storage abstractions and interfaces."
version: 0.0.1
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  app_core:
    path: ../app_core

dev_dependencies:
  flutter_test:
    sdk: flutter
  app_lints:
    path: ../app_lints
